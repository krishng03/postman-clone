import React from 'react';

const Home = () => {
    return (
        <div>
            <h2 className="text-2xl font-bold">Make you API calls here</h2>
            <p>This is a simple API call tool that allows you to make API calls to your own API or any other API.</p>
            <p>You can make GET, POST, PUT, DELETE, PATCH, and OPTIONS requests.</p>
        </div>
    );
}

export default Home;